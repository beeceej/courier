# Courier Client Server example

* run gRPC gen
    * `cd $GOPATH/src/github.com/beeceej/courier`
    * `./protogen.sh`
* Start server in a terminal window
    * `./server.sh`
* Run client.sh in another terminal window
    * `./client.sh`